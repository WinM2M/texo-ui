import { TEXO_BASE_STYLES } from '@texo-ui/react';

/**
 * Kept as a named re-export for backward compatibility. The stylesheet itself lives in
 * `@texo-ui/react` so the drop-in bundle and npm consumers cannot drift apart.
 */
export const DEFAULT_STYLES = TEXO_BASE_STYLES;
